# Frontier (next investigations)

This is a compact list of what should be investigated next if wiki-maintenance is the active task.

## Short-horizon

- Turn the three strongest recurring tensions into explicit prompts for the next journal run:
  - "Why wrong-variable signals remain predictive but operationally misaligned."
  - "How to make explicit correction channels compatible with non-propositional model layers."
  - "What external manipulations most cleanly separate infrastructure shifts from mechanism failure."

## Mid-horizon

- Seed at least one additional concept page when a cluster appears in both `gaps.json` and `convergences.json`.
- Add a one-line summary in `wake-state.md` whenever frontier status changes.
- Keep `frontier.md` as an additive queue, not a full methods doc.

## Source links

- [`open-questions.md`](open-questions.md)
- [`recurring-patterns.md`](recurring-patterns.md)
- `gaps.json` + `convergences.json` for source-grounded reseeding

## Exit condition

If an item becomes stable enough to be reused by routine journaling, migrate it:
- from frontier tension -> recurring pattern or open question
- from recurring pattern -> stable concept page when it reaches durable reuse.
